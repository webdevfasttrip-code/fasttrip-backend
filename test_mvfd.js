const axios = require('axios'); axios.post('http://localhost:5000/v1/search', { origin: 'DEL', destination: 'BOM', departureDate: '2026-05-20', adults: 1 }).then(res => { const flight = res.data.flights.find(f => f.supplier === 'FTSpecial'); if (flight) { console.log(JSON.stringify(flight.fareOptions[0].raw.priceDetails, null, 2)); } else { console.log('No MVFD flight'); } }).catch(e => console.log(e.message));
